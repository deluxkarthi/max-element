#include<stdio.h>
int main()
{
  int a[10],temp;
  int j;
  for(j=0;j<10;i++)
  { 
    scanf("%d",&a[j]);
  }
  temp=a[0];
  for(j=0;j<10;j++)
  {
    if(a[0]<a[j])
    {
      temp=a[j]);
    }
    printf("the largest number is %d",temp);
  }
